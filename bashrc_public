if [ -d ${HOME}/bin ] ; then
    PATH=${HOME}/bin:"${PATH}"
fi


#cd 自动纠错
shopt -s cdspell

#no dumps history
export HISTCONTROL=$HISTCONTROL${HISTCONTROL+,}ignoredups

#alias 
alias ..='cd ../'
alias ...='cd ../../'
